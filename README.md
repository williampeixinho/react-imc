# Calculadora de IMC (React)

Projeto em ReactJS que calcula o IMC a partir de altura (m) e peso (kg), exibindo o valor e a classificação conforme a tabela.

## Como rodar
```bash
npm install
npm run dev
